import { wrapLoadImages } from '../load-images';
import { ChipDisplayMain } from './main';

/**
 * Component which shows choosable list of chips.
 */
export const ChipDisplay = wrapLoadImages('images', ChipDisplayMain);
